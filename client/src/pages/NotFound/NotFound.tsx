import Error from "../../components/Error";
import Layout from "../../components/Layout";

const NotFound = () => {
  return (
    <Layout>
      <Error />
    </Layout>
  );
};

export default NotFound;
